name: bike_petrol_app
description: "bike_petrol_app - Built with Riverpod Architecture"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management & Dependency Injection
  # Riverpod is the recommended state management solution
  # Using NotifierProvider and AsyncNotifierProvider (no code generation needed)
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
